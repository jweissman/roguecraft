// need to refactor js to support testing witout phaser/a browser; also could mock window i guess?
// create object where we pass in phaser elements which we can mock...?
// would be nice to test with a 'real' window -- tempted to say necessary here actually
// so maybe that's the trick?
var assert = require("assert");
//var treasure = Roguecraft.treasure;

//describe('Array', function(){
//  describe('#indexOf()', function(){
//    it('should return -1 when the value is not present', function(){
//      assert.equal(-1, [1,2,3].indexOf(5));
//      assert.equal(-1, [1,2,3].indexOf(0));
//    })
//  })
//})

//describe("Roguecraft", function() {
//  describe("managing treasure", function() {
//    it('should be awesome', function() {
//      assert.equal(Roguecraft.awesome(), true);
//    })
//  })
//})
